[build]
  functions = "netlify/functions"
  publish = "build"
  command = "npm install --force && npm run build"

[build.environment]
  NPM_FLAGS = "--force"
  NODE_VERSION = "22"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200